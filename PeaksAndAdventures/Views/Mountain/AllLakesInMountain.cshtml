@model IEnumerable<AllLakesViewModel>

@{
	ViewBag.Title = "Езера";
}

<h2 class="text-center">@ViewBag.Title</h2>

<hr/>

@if	(Model.Any())
{
	@await Html.PartialAsync("_AllLakesPartial", Model)
}
else
{
	<h3 class="text-center">Няма добавени езера!</h3>	
}

