@model IEnumerable<PeaksAndAdventures.Core.Models.ViewModels.User.AllUsersViewModel> 

@{
	ViewData["Title"] = "Всички потребители";
}

<h3 class="text-center">@ViewData["Title"]</h3>

<hr/>

<table class="table table-striped table-bordered">
	<thead>
		<tr>
			<th>Потребителско име</th>
			<th>Имейл</th>
			<th>Изтриване</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var user in Model)
		{
			<tr>
				<td>@user.UserName</td>
				<td>@user.Email</td>
				<td>
					<a asp-area="" asp-controller="User" asp-action="Delete" asp-route-id="@user.Id" class="btn btn-danger">Изтрий</a>
				</td>
			</tr>
		}
	</tbody>
</table>
